# Ignore files cache.
force_downloads: False

# Directory were to keep files that need to be cached for addresses download.
# Note that if it is not inside a volume, the cache will probably be lost at
# each run.
cache_dir: /tmp/cache

tmp_dir: /tmp
